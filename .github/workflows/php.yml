name: Deploy PHP site to Hostinger

on:
 push:
  branches: [ "main" ]

jobs:
 deploy:
  runs-on: ubuntu-latest

  steps:
   - name: Checkout repository
     uses: actions/checkout@v4

   - name: Deploy via FTP
     uses: SamKirkland/FTP-Deploy-Action@v4.3.5
     with:
      server: ftp://92.113.24.87
      username: u449592101
      password: Tchouni.0102
      local-dir: ./
      server-dir: public_html/
